function crearUsr(nombre, apellido,fechaNacim,numero,correo){
    let usr = {
        nombre,
        apellido,
        fechaNacim,
        numero,
        correo,
    }
    console.log(usr);
    return usr;
}
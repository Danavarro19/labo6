var users = [];
function agregarUsr(nombre, apellido,fechaNacim,numero,correo){
    user = {
        nombre,
        apellido,
        fechaNacim,
        numero,
        correo,
    }
    users.push(user);
}